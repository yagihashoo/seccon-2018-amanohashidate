import Layout from '../../components/layout';
import {axiosWrapper} from '../../lib/utils';
import Router from 'next/router'

const Unsolved = (props) => (
    <Layout title={"Unsolved Challenges"}>
    </Layout>
)

export default Unsolved;
